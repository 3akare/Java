package Interfaces;

//An annotation adds metadata to an object and slightly alters how the compiler preceives the object
@FunctionalInterface
// This annotation is used for interfaces with only one method
// It also enables the use of lamda function ()->
public interface oopInterface {
    void speak();
}